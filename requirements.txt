# 核心基础依赖（无冲突，无需编译）
numpy==1.26.4
PyPDF2==3.0.1
Pillow==10.1.0
click==8.1.7
python-magic==0.4.27

# 向量数据库（指定稳定版，无依赖冲突）
chromadb==1.4.0

# 国内镜像加速配置（无需额外加参数，下载速度拉满）
-i https://pypi.tuna.tsinghua.edu.cn/simple